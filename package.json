{
  "name": "fullstack-graphql-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test:frontend": "turbo run test --scope=frontend",
    "lint:frontend": "turbo run lint --scope=frontend",
    "typecheck:frontend": "turbo run typecheck --scope=frontend"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^8.9.0",
    "prettier": "^2.5.1",
    "turbo": "1.1.5"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "yarn@1.22.17"
}
